import './Spacer.css';

function Spacer(props) {
    return (
      <div className="row" style={{height: `${props.contents}px`, backgroundColor:'reojd'}} >
      </div>
    );
  }
  
  export default Spacer;
  